<?xml version="1.0" encoding="UTF-8"?>
<?asciidoc-toc maxdepth="3"?>
<?asciidoc-numbered?>
<book xmlns="http://docbook.org/ns/docbook" xmlns:xl="http://www.w3.org/1999/xlink" version="5.0" xml:lang="en">
<info>
<title>AsciiSpec Setup</title>
<date>2017-01-30</date>
</info>
<chapter xml:id="_installation">
<title>Installation</title>
<simpara>Add AsciiSpec to your shell profile with the path to the <literal>asciispec/bin</literal> directory on your system:</simpara>
<simpara><literal>export PATH=$PATH:~/path/to/asciispec/bin/</literal></simpara>
<simpara>If the AsciiSpec tool is found by your system, running AsciiSpec with the <literal>-h</literal> flag will print a help page:</simpara>
<programlisting language="bash" linenumbering="unnumbered">asciispec -h</programlisting>
</chapter>
<chapter xml:id="_converting_documents" role="language-bash">
<title>Converting Documents</title>
<simpara>To convert this document with AsciiSpec, <literal>cd</literal> to the docs directory and run:</simpara>
<programlisting language="bash" linenumbering="unnumbered">asciispec userguide.adoc</programlisting>
<simpara>This will use the default HTML and create a file called <literal>userguide.html</literal>.<?asciidoc-br?>
To convert to HTML using a different CSS stylesheet, <link linkend="Document Attributes &amp; Variables">document attributes</link> are passed using the <literal>-a</literal> flag:</simpara>
<programlisting language="bash" linenumbering="unnumbered">asciispec -a stylesheet=mystyle.css userguide.adoc</programlisting>
<tip>
<simpara>Passing a document attribute via the command line will override that value if it has already been set in the source document.</simpara>
</tip>
<section xml:id="_docbook_xml">
<title>Docbook / XML</title>
<simpara>To convert to docbook (<literal>.xml</literal>), use the <literal>-b docbook</literal> flag:</simpara>
<simpara><literal>asciispec -b docbook userguide.adoc</literal></simpara>
<simpara>Most built-in CLI parameters are described in the help page by running <literal>asciispec -h</literal> and in further detail in the <link xl:href="http://asciidoctor.org/docs/user-manual/#cli-options">CLI Options</link> section of the AsciiDoctor user manual.</simpara>
</section>
<section xml:id="_pdf">
<title>PDF</title>
<simpara>Apache FOP (Formatting Objects Processor) is required for higher quality <emphasis role="strong">PDF</emphasis> generation.
A fork with custom PDF styling and syntax highlighting can be found at the following location:</simpara>
<simpara><link xl:href="https://github.com/NumberFour/asciidoctor-fopub">https://github.com/NumberFour/asciidoctor-fopub</link></simpara>
<orderedlist numeration="arabic">
<listitem>
<simpara>Clone the Asciidoctor Fopub repository</simpara>
<screen>git clone https://github.com/NumberFour/asciidoctor-fopub.git</screen>
</listitem>
<listitem>
<simpara>Add the <literal>asciidoctor-fopub/</literal> directory to your shell profile:</simpara>
<programlisting language="bash" linenumbering="unnumbered">export PATH=$PATH:~/path/to/asciidoctor-fopub/</programlisting>
</listitem>
<listitem>
<simpara>Confirm successful install using the <literal>which fopub</literal> command.</simpara>
<programlisting language="bash" linenumbering="unnumbered">$ which fopub
~/path/to/fopub</programlisting>
</listitem>
<listitem>
<simpara>Convert XML to PDF using the following:</simpara>
<programlisting language="bash" linenumbering="unnumbered">fopub myfile.xml</programlisting>
</listitem>
</orderedlist>
<simpara>An example command to convert from asciidoc to PDF would then look something like the following:</simpara>
<programlisting language="bash" linenumbering="unnumbered">asciispec -b docbook myfile.adoc &amp;&amp; fopub myfile.xml</programlisting>
</section>
<section xml:id="_configuration_file">
<title>Configuration File</title>
<simpara>AsciiSpec processors with configurable target URLs have to be set up by means of a configuration file.
For this case,<literal>config.adoc</literal> can be copied to the location of the source document and used as a template.
The following line of code must be included at the top of your source document:</simpara>
<simpara><literal>include::config.adoc[]</literal></simpara>
<tip>
<simpara>To configure a specific processor, see <link xl:href="https://numberfour.github.io/asciispec">AsciiSpec Processors</link>.</simpara>
</tip>
</section>
</chapter>
</book>
